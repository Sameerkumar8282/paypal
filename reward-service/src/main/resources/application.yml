server:
  port: 8089 # Reward service port

spring:
  application:
    name: reward-service

  kafka:
    bootstrap-servers: localhost:9092 #kafka server

    consumer:
      group-id: reward-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer #key deserializer
      value-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer #value deserializer
      properties:
        spring.deserializer.value.delegate.class: org.springframework.kafka.support.serializer.JsonDeserializer
        spring.json.trusted.packages: "*"
        spring.json.value.default.type: com.paypal.reward_service.entity.Transaction
        spring.json.use.type.headers: false

    listener:
      missing-topics-fatal: false